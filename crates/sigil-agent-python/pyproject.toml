[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "sigil-agent"
version = "0.1.0"
description = "Sigil Agent SDK — Nostr-based AI agent messaging"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
